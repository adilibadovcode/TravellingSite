@using TravellingSite.ViewModel.DestinationVM
@model List<DestinationListItemVM>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Index";
}
<div class="container">
    <a asp-action="Create" asp-controller="Destionation" class="btn btn-outline-success my-4">Create</a>
    <table class="table table-bordered">
        <thead>
            <tr class="text-center">
                <th scope="col">Id</th>
                <th scope="col">Image</th>
                <th scope="col">Title</th>
                <th scope="col">Price</th>
                <th scope="col">StarCount</th>
                <th colspan="2" scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @* @foreach (var item in Model)
            { *@
                <tr class="text-center">
                    @* <td>@item.Id</td>
                    <td>@item.Image</td>
                    <td>@item.Title</td>
                    <td>@item.Price</td>
                    <td>@item.StarCount</td> *@
                    <td><button type="button" class="btn btn-outline-primary">Update</button></td>
                    <td><button type="button" class="btn btn-outline-danger">Delete</button></td>
                </tr>
            @* } *@
        </tbody>
    </table>
</div>